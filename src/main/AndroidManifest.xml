<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".StudyMatcherApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_home"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_home"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyMatcher"
        android:usesCleartextTraffic="true">

        <!-- Splash -->
        <activity
            android:name=".auth.SplashActivity"
            android:exported="true"
            android:noHistory="true"
            android:theme="@style/Theme.StudyMatcher.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding -->
        <activity android:name=".auth.OnboardingActivity" android:exported="false" />

        <!-- Auth -->
        <activity android:name=".auth.RegisterActivity" android:exported="false" />
        <activity android:name=".auth.LoginActivity" android:exported="false" />

        <!-- Profile Setup -->
        <activity android:name=".profile.ProfileSetupActivity" android:exported="false" />
        <activity android:name=".profile.ProfileEditActivity" android:exported="false" />

        <!-- Subject Picker -->
        <activity android:name=".profile.SubjectPickerActivity" android:exported="false" />

        <!-- Main (host for fragments) -->
        <activity android:name=".ui.MainActivity" android:exported="false" />

        <!-- Chat -->
        <activity android:name=".chat.ChatDetailActivity" android:exported="false" />

        <!-- FCM Service -->
        <service
            android:name=".notification.StudyMatcherMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- FCM default channel -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="study_matcher_channel" />

    </application>

</manifest>
